# If you'd like to use keycloak as provisioned here, consider these values
#
# defaults
default_settings: &git_defaults
  repoURL: https://github.com/rhpds/ci-template-gitops.git
  targetRevision: main

deployer:
  domain: apps.cluster-guid.sandbox.opentlc.com
  apiUrl: https://api.cluster-guid.sandbox.opentlc.com:6443

tenant:
  name: xyzzy
  user:
    name: agv-generated-username # will be ignored in keycloak mode
    keycloakEnabled: true
    prefix: user
    count: 5
    names: []
    password: openshift
    # count: 2 # if we're deploying an event

# create some workspaces (namespaces)
workspace:
  enabled: false
  git:
    <<: *git_defaults
    path: tenant/workspace

# create an app in a namespace
# who doesn't need a loadtester?
loadtest:
  enabled: false
  requestsPerSecond: 0 #off
  duration: 1000
  url: https://example.com/your/app

# enable/disable entire lab suites
labs:
  rosetta:
    enabled: false

# Lab module values are in separate files:
#   values-lab-rosetta.yaml    - Rosetta lab modules
# Future labs follow the same pattern:
#   values-lab-<name>.yaml

# create a keycloak realm for this tenant
keycloakRealm:
  enabled: false
  git:
    <<: *git_defaults
    path: infra/keycloak/keycloak-realm
