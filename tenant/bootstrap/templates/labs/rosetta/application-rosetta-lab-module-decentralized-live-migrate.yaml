{{- $labRosetta := include "labRosetta" . | fromYaml -}}
{{ if and .Values.labs.rosetta.enabled $labRosetta.moduleDecentralizedLiveMigrate.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rosetta-lab-module-decentralized-live-migrate
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: tenants
  source:
    repoURL: {{ $labRosetta.moduleDecentralizedLiveMigrate.git.url }}
    targetRevision: {{ $labRosetta.moduleDecentralizedLiveMigrate.git.revision }}
    path: {{ $labRosetta.moduleDecentralizedLiveMigrate.git.path }}
  destination:
    server: https://kubernetes.default.svc
    namespace: vm-live-migration-source
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    managedNamespaceMetadata:
      labels:
        openshift.io/cluster-monitoring: "true"
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
