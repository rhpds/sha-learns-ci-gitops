{{ if .Values.mtcOperator.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mtc-operator
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ .Values.mtcOperator.git.repoURL }}
    targetRevision: {{ .Values.mtcOperator.git.targetRevision }}
    path: {{ .Values.mtcOperator.git.path }}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
